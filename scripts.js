let firstNumber = null;
let operator = null;
let secondNumber = null;
let result = null;
let isDecimal = false;

function operate (a, op, b) {
    if (op === '*') {
        return a * b;
    } else if (op === '/') {
        if (y === 0) {
            return NaN;
        } else {
            return a / b;
        }
    } else if (op === '+') {
        return a + b;
    } else if (op === '-') {
        return a - b;
    }
}

function percent (a) {
    return a / 100;
}

function ac (...args) {
    let firstNumber = null;
    let operator = null;
    let secondNumber = null;
    let result = null;
    let isDecimal = false;
}

function del (...args) {
    ;
}
function equal (...args) {
    ;
}
